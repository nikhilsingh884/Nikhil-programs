#include <stdio.h>
int main()
{
    int l, b;
    printf("Enter lenght: ");
    scanf("%d", &l);

    printf("Enter breadth: ");
    scanf("%d", &b);
    {
        printf("the area of rectangle is %d", l * b);
    }

    return 0;
}