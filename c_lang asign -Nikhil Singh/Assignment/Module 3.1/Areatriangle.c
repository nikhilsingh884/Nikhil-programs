#include <stdio.h>
int main()
{

    int h, b;

    printf("Enter height: ");
    scanf("%d", &h);

    printf("Enter base: ");
    scanf("%d", &b);

    printf("the area of the triangle is %d"), (h * b) * 0.5;

    return 0;
}